/**
 * Home Page Constants
 * Structural data only — copy lives in messages/{locale}/home.json.
 */

import type { StepItem } from "../types";

export const STEPS: StepItem[] = [
  { id: 1, icon: "click" },
  { id: 2, icon: "settings" },
  { id: 3, icon: "send" },
];
