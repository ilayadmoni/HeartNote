/**
 * RelationshipQuiz Constants
 * Question/editor bounds. Result feedback copy lives in the `templates`
 * message catalog (`relationshipQuiz.feedbackTiers`), resolved in QuizResults.
 */

/** Min/Max questions the editor allows */
export const MIN_QUESTIONS = 1;
export const MAX_QUESTIONS = 10;
