/**
 * Profile Page
 * User profile with subscription info, template usage, and created templates
 */

import { Profile } from "@/components/profile";

export default function ProfilePage() {
  return <Profile />;
}
